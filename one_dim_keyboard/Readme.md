# One Dimensional Keyboard
The problem is to find the optimum 1D keyboard for day to day life.
The dataset used (not shared here) is a slightly adjusted version of [this](https://www.kaggle.com/datasets/rtatman/english-word-frequency) dataset.
What I did was basically creating a new column named `freq` which is equal to `words`/sum(`words`).
